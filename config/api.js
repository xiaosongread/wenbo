
var publicjs = {
  // @if NODE_ENV = 'development'
  baseUrl: 'http://publisher-api-test.deepleaper.com/web',
  // @endif
  // @if NODE_ENV = 'production'
  baseUrl: 'http://publisher-api.deepleaper.com/web',
  // @endif
}